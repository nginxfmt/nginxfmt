# upstream backends
upstream backend {
    server 127.0.0.1:8080;
    server 127.0.0.1:8081;
}

server {
    listen 80;
    root "/var/www/html"; # web root

    location / {
        try_files $uri $uri/ =404;
    }
}
